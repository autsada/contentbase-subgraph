{
  "name": "contentbase-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "graph-local": "docker-compose up",
    "graph-local-clean": "docker-compose down -v && docker-compose rm -v && rm -rf data/ipfs data/postgres",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ contentbase-subgraph",
    "create-local": "graph create --node http://localhost:8020/ contentbase-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ contentbase-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 contentbase-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "^0.28.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
